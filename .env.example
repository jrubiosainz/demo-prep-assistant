# ----- No app registration required! -----
# Uses Azure CLI well-known client ID + device code flow.
# Just sign in with your Entra ID when prompted.

# Optional overrides:
# CLIENT_ID=your-client-id-if-you-have-one
# TENANT_ID=your-tenant-id

PORT=3000

# AI plan generation uses @github/copilot-sdk — authenticates
# automatically through your GitHub Copilot subscription.
# No manual tokens needed.
